# train and val datasets (image directory or *.txt file with image paths)
train: ./weights/detect/train/images
val: ./weights/detect/valid/images

# number of classes
nc: 1

# class names
names: ["lair"]